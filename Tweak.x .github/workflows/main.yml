#import <Foundation/Foundation.h>
#import <UIKit/UIKit.h>

typedef struct __IOHIDEvent *IOHIDEventRef;
extern uint32_t IOHIDEventGetType(IOHIDEventRef event);
extern float IOHIDEventGetFloatValue(IOHIDEventRef event, uint32_t field);

%hook IOHIDEventSystemClient
- (void)handleEvent:(IOHIDEventRef)event {
    if (IOHIDEventGetType(event) == 11) {
        float deltaX = IOHIDEventGetFloatValue(event, 0x0B0001);
        float deltaY = IOHIDEventGetFloatValue(event, 0x0B0002);#import <Foundation/Foundation.h>
#import <UIKit/UIKit.h>

typedef struct __IOHIDEvent *IOHIDEventRef;
extern uint32_t IOHIDEventGetType(IOHIDEventRef event);
extern float IOHIDEventGetFloatValue(IOHIDEventRef event, uint32_t field);

%hook IOHIDEventSystemClient
- (void)handleEvent:(IOHIDEventRef)event {
    if (IOHIDEventGetType(event) == 11) {
        float deltaX = IOHIDEventGetFloatValue(event, 0x0B0001);
        float deltaY = IOHIDEventGetFloatValue(event, 0x0B0002);
        NSLog(@"[RawMouse] DX: %f | DY: %f", deltaX, deltaY);
    }
    %orig;
}
%end

%hook BCWindowServerPointerController
- (void)setGlobalPointerOpacity:(double)arg1 {ARCHS = arm64 arm64e
TARGET := iphone:clang:latest:14.0
INSTALL_TARGET_PROCESSES = backboardd Roblox

include $(THEOS)/makefiles/common.mk

TWEAK_NAME = RawMouse
RawMouse_FILES = Tweak.x
RawMouse_CFLAGS = -fobjc-arc
export BUNDLE_ID = com.jsis.rawmouse
RawMouse_CODESIGN_FLAGS = -Sentitlements.plist

include $(THEOS_MAKE_PATH)/tweak.mk

    %orig(0.0);
}
%end

        NSLog(@"[RawMouse] DX: %f | DY: %f", deltaX, deltaY);
    }
    %orig;
}
%end

%hook BCWindowServerPointerController
- (void)setGlobalPointerOpacity:(double)arg1 {
    %orig(0.0);
}
%end
ARCHS = arm64 arm64e
TARGET := iphone:clang:latest:14.0
INSTALL_TARGET_PROCESSES = backboardd Package: com.jsis.rawmouse
Name: RawMousePackage: com.jsis.rawmouse
Name: RawMouse
Version: 0.1.0
Architecture: iphoneos-arm64
Description: Mouse de PC no iPad
Maintainer: jsis
Author: jsis
Section: Tweaks

Version: 0.1.0<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.developer.hid-event-access</key>
    <true/>
    <key>get-task-allow</key>
    <true/>
</dict>
</plist>name: Build-RawMouse
on: [push]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Theos
        run: |
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
          echo "THEOS=~/theos" >> $GITHUB_ENV
      - name: Compile
        run: |
          export THEOS=~/theos
          make package FINALPACKAGE=1
      - name: Save
        uses: actions/upload-artifact@v3
        with:
          name: RawMouse-Installer
          path: packages/*.deb


Architecture: iphoneos-arm64
Description: Mouse de PC no iPad
Maintainer: jsis
Author: jsis
Section: Tweaks
Roblox<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.developer.hid-event-access</key>
    <true/>
    <key>get-task-allow</key>
    <true/>
</dict>
</plist>


include $(THEOS)/makefiles/common.mk

TWEAK_NAME = RawMouse
RawMouse_FILES = Tweak.x
RawMouse_CFLAGS = -fobjc-arc
export BUNDLE_ID = com.jsis.rawmouse
RawMouse_CODESIGN_FLAGS = -Sentitlements.plist

include $(THEOS_MAKE_PATH)/tweak.mk
